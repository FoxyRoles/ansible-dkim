#!/bin/bash
set -e
source /build/buildconfig
set -x

# apt-get clean
# apt-get autoclean
# rm -rf /build
# rm -rf /var/lib/apt/lists/*
# rm -rf /tmp/* /var/tmp/*
# rm -rf /usr/share/man/??
# rm -rf /usr/share/man/??_*
